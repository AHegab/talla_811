// NOTE: CheckoutForm has been removed as requested.
// This file is intentionally stubbed to return null to avoid import errors.
// The original implementation is archived in CheckoutForm.removed.md.
// Users are redirected directly to Shopify hosted checkout using cart.checkoutUrl.

export function CheckoutForm() {
  return null;
}
