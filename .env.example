# ================================
# Shopify Configuration
# ================================

# Your Shopify store domain (without https://)
PUBLIC_STORE_DOMAIN=your-store.myshopify.com

# Public Storefront API token for client-side requests
# Get this from: Shopify Admin > Settings > Apps and sales channels > Develop apps > [Your app] > API credentials
PUBLIC_STOREFRONT_API_TOKEN=your_public_storefront_api_token

# Checkout domain (usually same as store domain)
PUBLIC_CHECKOUT_DOMAIN=your-store.myshopify.com

# Hydrogen Storefront ID
PUBLIC_STOREFRONT_ID=gid://shopify/HydrogenStorefront/1

# Private Storefront API token for server-side requests (recommended for production)
# Get this from: Shopify Admin > Settings > Apps and sales channels > Develop apps > [Your app] > API credentials
PRIVATE_STOREFRONT_API_TOKEN=your_private_storefront_api_token

# ================================
# Email Service Configuration
# ================================

# SendGrid API Key for email notifications
# Get your key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key

# Resend API Key (alternative email service)
# Get your key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key

# ================================
# Database Configuration
# ================================

# MongoDB connection string for analytics and notifications
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_DATABASE=talla_analytics

# Enable/disable analytics tracking
ANALYTICS_ENABLED=true
